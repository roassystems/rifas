
const Usuario = require('./usuario');
const Role = require('./role');
const Parametros = require('./parametros');
const Sorteos = require('./sorteos');
const Beneficiario = require('./beneficiarios');
const Documentos = require('./documentos');
const PagoMovilUsuario = require('./pagomovil');
module.exports = {
    // Categoria,
    // Producto,
    Role,
    Parametros,
    Sorteos,
    // Server,
    Usuario,
    Beneficiario,
    Documentos,
    PagoMovilUsuario
}
